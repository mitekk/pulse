# API base URL (used for reference only; the Vite proxy handles /api forwarding in dev)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# WebSocket URL (used for Socket.IO connection in Phase 2)
VITE_WS_URL=http://localhost:3000
