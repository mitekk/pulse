import { EmptyState } from '@/components/EmptyState'
export default function ExplorePage() {
  return <EmptyState title="Explore" description="Trends, hashtags, and discovery — coming in Phase 5." />
}
