import { EmptyState } from '@/components/EmptyState'
export default function SearchPage() {
  return <EmptyState title="Search" description="Full-text search across posts and people — coming in Phase 5." />
}
