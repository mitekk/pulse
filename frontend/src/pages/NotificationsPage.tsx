import { EmptyState } from '@/components/EmptyState'
export default function NotificationsPage() {
  return <EmptyState title="Notifications" description="Likes, replies, follows, and more — coming in Phase 5." />
}
