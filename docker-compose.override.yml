services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend/src:/app/src
    command: ["npx", "tsx", "watch", "src/main.ts"]
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"
